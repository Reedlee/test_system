<div class="page-header">
  <%= link_to 'Tests', tests_path %> > <%= @test.name %>
  <h1>Questions for "<%= @test.name %>"</h1>
  <%= link_to 'New question', new_question_path, class: 'btn btn-default btn-lg' %>
</div>

<table class="table table-bordered">
  <tr>
    <td>Question</td>
    <td>Answers</td>
    <td>Actions</td>
  </tr>

  <% @questions.each do|question|%>
      <tr>
        <td><%=  question.name %></td>
        <td><%= link_to question.answers.count, answer_path(question)  %></td>
        <td>
          <%= link_to 'Edit,', edit_test_path(test)  %>
          <%=link_to 'Remove', test_path(test), method: :delete %>
        </td>
        </td>
      </tr>
  <% end %>
</table>
